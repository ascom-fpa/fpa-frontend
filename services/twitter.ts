import api from "./axios"

export const getRecentTweets = async (query: string): Promise<any> => {
  // const response = await api.get(`/twitter/search?q=${encodeURIComponent(query)}`)
  // return response.data
}